seeds:
  - name: city-region
    description: Сопоставление регионов и городов
    config:
      delimiter: ";"
      column_types:
        city: varchar(50)
        region: varchar(50)
    columns:
      - name: city
        tests:
          - not_null
          - unique